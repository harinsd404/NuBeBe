from database import Base

# 여기에 SQLAlchemy 모델들을 정의하시면 됩니다.
# 예시:
# from sqlalchemy import Column, Integer, String
#
# class User(Base):
#     __tablename__ = "users"
#     id = Column(Integer, primary_key=True, index=True)
#     username = Column(String, unique=True, index=True)
